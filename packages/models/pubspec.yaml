name: models
description: Shared domain models for the RMS Flutter monorepo (freezed + json_serializable).
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  freezed_annotation: "^2.4.4"
  json_annotation: "^4.9.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: "^2.4.13"
  freezed: "^2.5.7"
  json_serializable: "^6.8.0"
  # fast_check: "^0.2.0"  # Added in task 3.3
